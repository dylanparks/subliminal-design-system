{
  "name": "subliminal-design-system-monorepo",
  "private": true,
  "workspaces": [
    "design-system",
    "site"
  ],
  "scripts": {
    "build:site": "npm run build:lib --workspace=design-system && npm run build --workspace=site"
  }
}
