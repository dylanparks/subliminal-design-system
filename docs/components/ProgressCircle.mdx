---
title: ProgressCircle
category: DataDisplay
sourcePath: design-system/src/components/DataDisplay/ProgressCircle/ProgressCircle.tsx
storiesPath: design-system/src/components/DataDisplay/ProgressCircle/ProgressCircle.stories.tsx
status: stub
---

# ProgressCircle

> 📋 **Full documentation coming soon.** This page is a placeholder — props and a Storybook
> link only. For anatomy, variants, token dependencies, accessibility notes, and Figma/code/
> Claude usage guidance, check back after the docs site backfill (see `docs/PROJECT_PLAN.md`).

**Storybook:** `DataDisplay/ProgressCircle`

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `value` | `number \| null` | — | Current progress value. Pass `null` or omit for indeterminate (animated spinner). Accepts values between `min` and `max` |
| `min` | `number` | `0` | Minimum value |
| `max` | `number` | `100` | Maximum value |
| `size` | `'xsmall' \| 'small' \| 'medium' \| 'large'` | `'small'` | Visual size of the circle |
| `propStatic` | `boolean` | `false` | White-palette variant for dark or gradient backgrounds |
| `aria-label` | `string` | — | Accessible label — required for screen readers; defaults to "Loading" when indeterminate |
| `className` | `string` | — | Additional className forwarded to the root element |
