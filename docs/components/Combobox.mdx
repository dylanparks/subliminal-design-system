---
title: Combobox
category: Fields
sourcePath: design-system/src/components/Fields/Combobox/Combobox.tsx
storiesPath: design-system/src/components/Fields/Combobox/Combobox.stories.tsx
status: stub
---

# Combobox

> 📋 **Full documentation coming soon.** This page is a placeholder — props and a Storybook
> link only. For anatomy, variants, token dependencies, accessibility notes, and Figma/code/
> Claude usage guidance, check back after the docs site backfill (see `docs/PROJECT_PLAN.md`).

**Storybook:** `Fields/Combobox`

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `label` | `string` | `'Select'` | Floating label shown inside the field (large/medium) or above (small) |
| `value` | `string` | — | Controlled selected value |
| `defaultValue` | `string` | — | Uncontrolled default selected value |
| `onChange` | `(value: string) => void` | — | Called when an option is committed |
| `options` | `ComboboxOption[]` | `[]` | List of options to show in the dropdown, each with a `value` and `label` |
| `placeholder` | `string` | `''` | Placeholder text shown in the input when open and empty |
| `size` | `'large' \| 'medium' \| 'small'` | `'large'` | Visual height: large = 64px · medium = 56px · small = 48px |
| `disabled` | `boolean` | `false` | Disables all interaction |
| `error` | `boolean` | `false` | Shows error treatment — takes priority over success |
| `success` | `boolean` | `false` | Shows success treatment |
| `message` | `string` | — | Helper / validation message shown below the field |
| `name` | `string` | — | HTML hidden input name attribute (for form submission) |
| `className` | `string` | — | Additional className forwarded to the root element |
| `aria-label` | `string` | — | Accessible label override (falls back to label prop) |
