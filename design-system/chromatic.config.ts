// Chromatic configuration
// Accessibility tests are enabled automatically when @chromatic-com/storybook
// and @storybook/addon-a11y are both present. Chromatic captures axe violations
// from every story snapshot and surfaces them in the Chromatic UI.
//
// Per-story opt-out:  parameters: { chromatic: { disableSnapshot: true } }
// Per-story a11y off: parameters: { a11y: { disable: true } }
export default {};
